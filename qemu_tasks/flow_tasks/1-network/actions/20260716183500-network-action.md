# Action: Remove browser mode of network

- Lane: qemu
- Agent: claude-opus-orchestrator
- Date: 2026-07-16
- Inputs read: [brief.md, plans/20260716183139-network-plan.md, explains/20260716183200-network-explain.md, grills1/20260716183300-network-grill1.md, qemu/index.html, qemu/arg-module.js, qemu/out.js]
- Status: ready
- Tags: [console, qemu, network, css, action]

## Purposes/Goals

- [x] Remove browser network mode from `qemu/index.html`.
- [x] Replace `?net=` query gating with a Phase-1 startup modal.
- [x] Empty input truly disables network (Grill 1 F1 resolved).
- [x] Normalize proxy URL scheme (Grill 1 F2).
- [x] Load terminal + wasm in Phase 2 after modal answered.
- [x] Apply cyberpunk theme.

## Conclusions

Single file changed: `qemu/index.html` (full rewrite of markup + script).

### Changes made

1. **Removed browser mode entirely**: deleted the `browser` branch, `Stack.Start`,
   the `stack-worker.js` / `c2w-net-proxy.wasm.gzip` references, the
   `192.168.127.253` proxy env block, `getNetParam()` (URL `?net=` parsing),
   and the `<script src="./dist/stack.js"></script>` tag (Grill 1 F3 confirmed
   `Stack` has no other consumer).

2. **Phase 1 — cyberpunk modal** (`#net-modal`): neon card with a single text
   input (`#net-url`, placeholder `ws://localhost:8888`), a primary
   "Connect & boot" button, and a ghost "No network" button. Enter submits.
   Input is autofocused. Helper text states empty = network disabled.

3. **Phase 2 — `bootWithNet(netUrl)`**: builds the xterm + pty, sets
   `Module.pty` / `mainScriptUrlOrBlob`, assembles the `info` blob, then calls
   the **unchanged** `start(info)`. Order within Phase 2 matches the original
   top-level order (xterm -> openpty -> Module.pty -> start), so the pty poll /
   onReadable preRun hooks are still registered before `initEmscriptenModule`.

4. **CSS**: inline `<style>` cyberpunk theme (neon cyan/magenta/violet, grid
   backdrop, glow shadows) for `body`, `#terminal`, and the modal.

### Grill 1 findings — resolution

- **F1 (blocker) RESOLVED**: added `disableGuestNetwork()` which splices the
  `-netdev socket,... -device virtio-net-pci,...` pair out of
  `Module['arguments']` (set by `arg-module.js`) when the URL is empty. Verified
  against `out.js:3730-3733`: with the NIC removed the guest never opens the
  fallback `ws://127.0.0.1:8888`, so an air-gapped boot cannot hang on a dead
  proxy. Fix stays inside `index.html`; `arg-module.js` untouched.
- **F2 (risk) RESOLVED**: `normalizeUrl()` trims input and prepends `ws://`
  when the user omits the scheme (SOCKFS rejects bare `host:port`). `localhost`
  is left as typed (matches the task's example).
- **F3 (nit)**: acted on — `dist/stack.js` tag removed.
- **F4 (risk) ACCEPTED, mitigated**: terminal is created in Phase 2 per the
  literal task wording, but Phase-2 statement order preserves the original
  pty/preRun sequencing, so the ordering risk does not materialize.
- **F5 (nit)**: autofocus + Enter-submit added. No full focus-trap (single
  input, low value).
- **F6/F7 (nits)**: COOP/COEP untouched (`server.mjs` unchanged). Empty-input
  boot verification handed to the Test phase.

### Files changed

- `qemu/index.html` — rewritten.

### Deviations from plan

- Plan floated a single "Start" button; shipped two explicit buttons
  ("Connect & boot" / "No network") for clarity, plus Enter-to-submit.
- Added `disableGuestNetwork()` (not in original plan) to resolve Grill 1 F1.

### Commands run

- `grep` for forbidden refs (Stack/stack.js/9999/192.168.127/c2w-net-proxy/
  getNetParam/browser) — only remaining "browser" hit is the page `<title>`.
- Node syntax check attempted but **node is not installed on this host**
  (`node`/`nodejs` absent, no nvm). Flagged for the Test phase.

## Constraints/Dependencies

- Only `qemu/index.html` edited; no generated artifacts touched.
- Real `ping` test needs `node server.mjs` + a browser + a live delegate proxy
  on the given WS URL. Host currently lacks node.

## Follow-ups

- Grill 2 adversary: review module boundaries / lifecycle / hidden coupling.
- Test: verify empty-input boot (no network) and `ping 1.1.1.1` with a proxy;
  if node/browser/proxy unavailable, record exact blocker + next command.
