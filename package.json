{
  "name": "c2w-ubuntu-tmp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Run ubuntu:22.04 in the browser via container2wasm, with the container's /tmp backed by a per-session UUID bucket in OPFS.",
  "scripts": {
    "start": "node server.mjs",
    "proxy": "c2w-net --listen-ws localhost:8888"
  }
}
